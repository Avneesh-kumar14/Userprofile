
  # User Profile

  This is a code bundle for User Profile. The original project is available at https://www.figma.com/design/1V4mkm9vMQmDltHegB8afK/User-Profile.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.

  build by the figma ai  bot for  component and designed feature in the webiste 
